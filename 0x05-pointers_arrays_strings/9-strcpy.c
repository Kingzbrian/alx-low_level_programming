#include "main.h"
/**
 * _strcpy - copy one string to annother
 * @dest: destunation
 * @src: source
 * Return: destination
 */
char *_strcpy(char *dest, char *src)
{
	int i;

	i = src;
	dest = i;
	return (dest);
}
