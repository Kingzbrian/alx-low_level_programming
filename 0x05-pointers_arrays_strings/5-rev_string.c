#include "main.h"
/**
 * rev_string - reverse a string
 * @s: pointer
 * Return: reversed string
 */
void rev_string(char *s)
{
	int i;
	int rev;

	i = 0;
	while (s[i])
	{
		i++;
	}
	rev = i;
	while (i--)
	{
		return (rev);
	}
}
